---
phase: 02-patient-management
plan: 05
title: "Patient Search"
type: execute
wave: 3
depends_on:
  - "02-01"
files_modified:
  - "app/services/patient_search.py"
  - "app/schemas/search.py"
  - "app/api/v1/patient_search.py"
  - "app/api/v1/router.py"
  - "tests/test_search.py"
  - "tests/conftest.py"
autonomous: true
requirements:
  - PAT-05
must_haves:
  truths:
    - "Patient search returns results in under 2 seconds"
    - "pg_trgm GIN indexes enable fuzzy search across name, MRN, phone (D-15)"
    - "Typeahead with 300ms debounce, 4-character minimum (D-16, D-19)"
    - "Unified search box + advanced filter panel (D-17)"
    - "50 results per page with pagination (D-20)"
    - "Every search query is audit-logged with who, what, when (D-68)"
  artifacts:
    - path: "app/services/patient_search.py"
      provides: "PatientSearchService with pg_trgm search, audit logging"
      min_lines: 100
    - path: "app/schemas/search.py"
      provides: "PatientSearchParams, PatientSearchResponse"
      min_lines: 40
    - path: "app/api/v1/patient_search.py"
      provides: "Search endpoint (unified + advanced)"
      min_lines: 80
  key_links:
    - from: "app/services/patient_search.py"
      to: "app/models/patient.py"
      via: "Search queries the Patient model with pg_trgm similarity"
      pattern: "func\\.similarity|ilike"
    - from: "app/services/patient_search.py"
      to: "app/services/audit.py"
      via: "AuditService.log_event for every search (D-68)"
      pattern: "AuditService\\.log_event"
    - from: "app/api/v1/patient_search.py"
      to: "app/services/patient_search.py"
      via: "API endpoint delegates to PatientSearchService"
      pattern: "PatientSearchService\\."
---

## Phase Goal

**As a** front desk or clinical staff member, **I want to** quickly search for patients by name, MRN, phone, or date of birth with sub-2-second response times, **so that** I can locate any patient record instantly during clinical workflows.

---

<execution_context>
@C:/Users/ABDUL/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ABDUL/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-patient-management/02-CONTEXT.md
@.planning/phases/02-patient-management/02-RESEARCH.md
@.planning/phases/02-patient-management/02-PATTERNS.md
@.planning/phases/02-patient-management/02-01-SUMMARY.md

<interfaces>
<!-- Key types from Plan 02-01 that this plan consumes -->

From app/models/patient.py (created in 02-01):
```python
class Patient(Base):
    __tablename__ = "patients"
    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    mrn: Mapped[str]  # indexed, unique
    first_name: Mapped[str]
    last_name: Mapped[str]
    date_of_birth: Mapped[date]
    gender: Mapped[str]
    phone: Mapped[str]
    status: Mapped[str]  # active/inactive/deceased
```

From app/models/__init__.py (GIN indexes created in migration 02-01):
```python
# Migration creates:
# ix_patient_name_trgm ON gin ((first_name || ' ' || last_name) gin_trgm_ops)
# ix_patient_mrn_trgm ON gin (mrn gin_trgm_ops)
# ix_patient_phone_trgm ON gin (phone gin_trgm_ops)
```

From app/services/audit.py (AuditService.log_event signature):
```python
@staticmethod
async def log_event(
    db: AsyncSession,
    user_id: uuid.UUID | None,
    action: str,
    resource: str,
    resource_id: str | None = None,
    metadata: dict | None = None,
    patient_id: uuid.UUID | None = None,
    why: str | None = None,
) -> AuditLog:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search service, schemas, API endpoint, and tests</name>
  <files>
    app/services/patient_search.py,
    app/schemas/search.py,
    app/api/v1/patient_search.py,
    app/api/v1/router.py,
    tests/test_search.py,
    tests/conftest.py
  </files>
  <read_first>
    app/models/patient.py,
    app/services/rbac.py,
    app/services/audit.py,
    app/api/v1/auth.py,
    app/api/v1/router.py,
    app/middleware/permission.py,
    tests/conftest.py
  </read_first>
  <action>
**app/schemas/search.py** — Search schemas:

1. PatientSearchParams(BaseModel):
   - q: str = Field(..., min_length=4) — D-19: 4 char minimum
   - page: int = Field(default=1, ge=1)
   - page_size: int = Field(default=50, ge=1, le=100) — D-20: 50 per page
   - status: str | None = None — filter by active/inactive/deceased
   - gender: str | None = None — filter by Male/Female
   - date_from: date | None = None — DOB range start
   - date_to: date | None = None — DOB range end
   - state: str | None = None — address state filter
   - city: str | None = None — address city filter

2. PatientSearchResult(BaseModel):
   - id: str
   - mrn: str
   - first_name: str
   - last_name: str
   - date_of_birth: date
   - gender: str
   - phone: str
   - status: str
   - relevance_score: float | None = None — pg_trgm similarity score
   - model_config = ConfigDict(from_attributes=True)

3. PatientSearchResponse(BaseModel):
   - results: list[PatientSearchResult]
   - total: int
   - page: int
   - page_size: int
   - total_pages: int

**app/services/patient_search.py** — PatientSearchService:

class PatientSearchService:
    """Patient search using PostgreSQL pg_trgm (D-15). Audit-logged (D-68)."""

    @staticmethod
    async def search_patients(
        db: AsyncSession,
        params: PatientSearchParams,
        user_id: uuid.UUID,
    ) -> PatientSearchResponse:
        """Search patients by name, MRN, phone, DOB with pg_trgm similarity.

        D-15: pg_trgm for fuzzy search
        D-19: 4-character minimum (enforced by schema)
        D-20: 50 results per page
        D-68: Log every search with who, what query, when
        """
        # Build search condition using ilike for SQLite compatibility
        # In production with PostgreSQL, pg_trgm similarity is used via GIN indexes
        query = params.q
        search_pattern = f"%{query}%"

        # Base query with search conditions (ilike works on both SQLite and PostgreSQL)
        stmt = select(Patient).where(
            or_(
                Patient.mrn.ilike(search_pattern),
                Patient.first_name.ilike(search_pattern),
                Patient.last_name.ilike(search_pattern),
                Patient.phone.ilike(search_pattern),
                # For PostgreSQL: func.similarity(Patient.first_name, query) > 0.3
            )
        )

        # Apply optional filters (D-17: advanced filters)
        if params.status:
            stmt = stmt.where(Patient.status == params.status)
        if params.gender:
            stmt = stmt.where(Patient.gender == params.gender)
        if params.date_from:
            stmt = stmt.where(Patient.date_of_birth >= params.date_from)
        if params.date_to:
            stmt = stmt.where(Patient.date_of_birth <= params.date_to)
        if params.state:
            stmt = stmt.where(Patient.address_state.ilike(f"%{params.state}%"))
        if params.city:
            stmt = stmt.where(Patient.address_city.ilike(f"%{params.city}%"))

        # Order by relevance (name match first, then MRN, then phone)
        stmt = stmt.order_by(Patient.last_name, Patient.first_name)

        # Count total
        count_stmt = select(func.count()).select_from(stmt.subquery())
        total = (await db.execute(count_stmt)).scalar()

        # Paginate (D-20: 50 per page)
        offset = (params.page - 1) * params.page_size
        result = await db.execute(stmt.offset(offset).limit(params.page_size))
        patients = list(result.scalars().all())

        # D-68: Audit every search — log who, what query, when, results count
        await AuditService.log_event(
            db,
            user_id=user_id,
            action="patient_search",
            resource="patient",
            metadata={
                "query": query,
                "filters": {
                    "status": params.status,
                    "gender": params.gender,
                    "date_from": str(params.date_from) if params.date_from else None,
                    "date_to": str(params.date_to) if params.date_to else None,
                    "state": params.state,
                    "city": params.city,
                },
                "results_count": total,
                "page": params.page,
            },
            why="Patient search",
        )

        total_pages = (total + params.page_size - 1) // params.page_size

        return PatientSearchResponse(
            results=[
                PatientSearchResult(
                    id=str(p.id),
                    mrn=p.mrn,
                    first_name=p.first_name,
                    last_name=p.last_name,
                    date_of_birth=p.date_of_birth,
                    gender=p.gender,
                    phone=p.phone,
                    status=p.status,
                )
                for p in patients
            ],
            total=total,
            page=params.page,
            page_size=params.page_size,
            total_pages=total_pages,
        )

**app/api/v1/patient_search.py** — Search endpoint:

router = APIRouter(prefix="/patients/search", tags=["patient-search"])

Endpoints:
1. GET /patients/search (response_model=PatientSearchResponse)
   - require_permission("patient", "read")
   - Query params: q (str, min 4), page, page_size, status, gender, date_from, date_to, state, city
   - Build PatientSearchParams from query params
   - Call PatientSearchService.search_patients(db, params, current_user.id)
   - Return results

2. GET /patients/search/typeahead (returns list of basic patient info)
   - require_permission("patient", "read")
   - Query param: q (str, min 4)
   - Return top 10 results with just id, mrn, name, status (for autocomplete)
   - Used by HTMX typeahead on the patient list page

**app/api/v1/router.py** — Add search router:
```python
from app.api.v1.patient_search import router as patient_search_router
router.include_router(patient_search_router)
```

**tests/test_search.py** — Write comprehensive tests:
1. test_search_by_name — Create patient "John Smith", search "john", assert found
2. test_search_by_mrn — Create patient with MRN LAG-000001, search "LAG-000", assert found
3. test_search_by_phone — Create patient with phone "08012345678", search "0801", assert found
4. test_search_minimum_length — Search with 3 chars, assert 422 validation error (D-19)
5. test_search_pagination — Create 60 patients, search with page_size=50, assert total=60, results=50, page 2 has 10
6. test_search_with_status_filter — Create active + inactive patients, filter status='active', assert only active returned
7. test_search_with_gender_filter — Create male + female patients, filter gender='Male', assert only male returned
8. test_search_audit_log — Perform search, verify audit log with action='patient_search', query, results_count
9. test_search_no_results — Search for "ZZZZZNONEXISTENT", assert 0 results
10. test_typeahead — Call /typeahead, assert returns top 10 with id, mrn, name only
11. test_search_performance — Create 1000 patients (bulk), search, assert response in <2 seconds
  </action>
  <verify>
    <automated>cd C:\Users\ABDUL\Desktop\Projects\HMS && python -m pytest tests/test_search.py -x -v 2>&1 | head -60</automated>
  </verify>
  <done>PatientSearchService with ilike-based search (pg_trgm-ready), advanced filters (D-17), pagination (D-20), and audit logging (D-68). Two API endpoints: unified search + typeahead. 11+ tests covering name/MRN/phone search, minimum length, pagination, filters, audit logging, and performance. Router registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Search query strings cross browser to FastAPI; SQL injection risk if not parameterized |
| API → Database | pg_trgm/indexed queries execute against patient table; PHI access event |
| API → Audit | Every search query logged with user, query terms, result count (D-68) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Tampering | SQL injection via search query | mitigate | SQLAlchemy ORM parameterized queries; ilike with bound parameters; never raw SQL with user input |
| T-02-19 | Information Disclosure | Search results exposure to unauthorized users | mitigate | require_permission("patient", "read") on all search endpoints; search audit logged per D-68 |
| T-02-20 | Information Disclosure | Search query logging captures PHI | accept | D-68 requires search audit logging; queries may contain patient name fragments; mitigated by audit log access controls |
| T-02-21 | Elevation of Privilege | Rate-bypass via rapid search | mitigate | slowapi rate limiter available (from Phase 1); can be applied to search endpoints if abuse detected |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed; pg_trgm is PostgreSQL extension |
</threat_model>

<verification>
1. All tests pass: `python -m pytest tests/test_search.py -x -v`
2. Service works: `python -c "from app.services.patient_search import PatientSearchService"`
3. Schemas validate: `python -c "from app.schemas.search import PatientSearchParams, PatientSearchResponse"`
4. API routes registered: Check router.py includes patient_search_router
5. Search audit: Every search produces an AuditLog entry with action='patient_search'
6. 4-char minimum: Search with 3 chars returns 422
</verification>

<success_criteria>
- Search returns results using ilike (pg_trgm-ready for PostgreSQL)
- Advanced filters: status, gender, DOB range, state, city (D-17)
- 4-character minimum enforced (D-19)
- 50 results per page with pagination (D-20)
- Every search audit-logged with who, what, when, results count (D-68)
- Typeahead endpoint for HTMX autocomplete
- 11+ tests passing
</success_criteria>

<output>
Create `.planning/phases/02-patient-management/02-05-SUMMARY.md` when done
</output>
